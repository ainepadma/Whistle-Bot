# 小鹞 WhistleBot 预览版 v1.0.0

> 版本标识：**WhistleBot v1.0.0**  
> 发布者：**Ainepadma**  
> 研发团队：**东南大学 声绘鹞影实践团**  
> 发布日期：2026-08-15  
> 平台：仅支持 Windows 10 / 11 x64

这是小鹞 WhistleBot 的首个公开发布预览版。一只由三维哨口模型驱动的小鹞会常驻桌面，根据你的互动、键盘输入和系统状态做出多种表情回应。

## 下载

| 文件 | 说明 | 大小 |
|---|---|---|
| `DesktopPet-win-x64-setup.exe` | 安装包（推荐） | 约 2.3 MB |
| `DesktopPet-win-x64.zip` | 绿色版压缩包，解压即用 | 约 0.5 MB |

两个版本均双击 `Bootstrap.exe` 启动。

## 系统要求

- Windows 10 / 11 64 位
- .NET 9 Desktop Runtime（首次运行会自动引导安装，免管理员权限）
- WebView2 运行时（Windows 10/11 一般已内置）

## 安装与运行

安装包：

1. 运行 `DesktopPet-win-x64-setup.exe`；
2. 按向导完成安装；
3. 安装完成后自动启动，也可通过开始菜单或桌面快捷方式再次打开。

绿色版：

1. 解压 `DesktopPet-win-x64.zip`；
2. 双击文件夹中的 `Bootstrap.exe`。

## 使用说明

- 拖动宠物可移动位置；
- 左键点击宠物可与它互动；
- 右键宠物打开菜单：
  - 样式：更换颜色、切换小 / 中 / 大尺寸
  - 设置：开机自启动、创建桌面快捷方式、卸载
  - 隐藏、退出
- 右下角托盘图标可随时显示或退出桌宠。

### 表情行为

| 场景 | 触发条件 |
|---|---|
| 出现 | 启动时 |
| 待机 | 默认状态 |
| 无互动 | 2 分钟未碰宠物 |
| 互动 | 鼠标进入 / 点击宠物 |
| 敲键盘 | 检测到键盘输入 |
| 系统待机 | 电脑空闲超过 5 分钟 |
| 消失 | 退出时播放动画 |

## 本次更新（v1.0.0 预览版）

- 首个公开发布预览版，启用新品牌“小鹞 WhistleBot”
- 右键菜单体验优化：以右键位置为锚点、原地展开、屏幕边缘自动翻转、无滚动条、不闪烁
- 菜单独立窗口化，不再被模型区域裁切
- 表情行为状态机：待机 / 互动 / 键盘输入 / 系统待机等场景自动切换

## 已知限制

- 仅支持 Windows x64 单主屏环境；
- 首次运行需要联网下载 .NET 9 Runtime（如未安装）；
- 预览版仍在打磨，个别场景下表情切换或菜单定位可能不够理想，欢迎反馈。

## 反馈

遇到问题或有建议，请通过 GitHub Issues 提交，并附上：

- 操作系统版本（Win10 / Win11 与版本号）
- 屏幕分辨率与缩放比例
- 问题出现时的操作步骤

源码与构建说明见 [README.md](README.md)。
