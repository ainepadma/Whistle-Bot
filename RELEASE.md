# 小鹞 WhistleBot 预览版 v1.1.0

> 版本标识：**WhistleBot-Preview v1.1.0**  
> 开发人员：**Ainepadma**  
> 研发团队：**东南大学 声绘鹞影实践团**  
> 发布日期：2026-08-16  
> 平台：仅支持 Windows 10 / 11 x64

这是小鹞 WhistleBot 的第二个公开发布预览版。一只由三维哨口模型驱动的小鹞会常驻桌面，根据你的互动、键盘输入和系统状态做出多种表情回应，并内置番茄钟专注计时与敲键盘音符动画。

## 下载

| 文件 | 说明 | 大小 |
|---|---|---|
| `DesktopPet-win-x64-setup.exe` | 安装包（推荐） | 约 2.3 MB |
| `DesktopPet-win-x64.zip` | 绿色版压缩包，解压即用 | 约 0.5 MB |

两个版本均双击 `Bootstrap.exe` 启动。

## 系统要求

- Windows 10 / 11 64 位
- .NET 9 Desktop Runtime（首次运行会自动引导安装，免管理员权限）
- WebView2 运行时（Windows 10/11 一般已内置）

## 安装与运行

安装包：

1. 运行 `DesktopPet-win-x64-setup.exe`；
2. 按向导完成安装；
3. 安装完成后自动启动，也可通过开始菜单或桌面快捷方式再次打开。

绿色版：

1. 解压 `DesktopPet-win-x64.zip`；
2. 双击文件夹中的 `Bootstrap.exe`。

## 使用说明

- 拖动宠物可移动位置；
- 左键点击宠物可与它互动；
- 右键宠物打开菜单：
  - 样式：更换颜色、切换小 / 中 / 大尺寸（模型 150 / 200 / 250，容器 200 / 250 / 300，默认小）
  - 设置：开机自启动、创建桌面快捷方式、关于、卸载
  - 专注：打开番茄钟浮层（25/5、45/10、60/15 预设 + 自定义时长与轮数，专注完成自动进入休息）
  - 隐藏、退出
- 右下角托盘图标可随时显示或退出桌宠。

### 专注计时

- 右键菜单选择“专注”打开计时浮层，可开始 / 暂停 / 继续 / 重置 / 跳过；
- 预设支持 25/5、45/10、60/15；选“自定义”可手动设置专注、休息、长休分钟数及每几轮进入长休；
- 专注完成自动进入休息，休息结束自动开始下一轮，每完成 4 轮进入一次长休息；
- 关闭浮层或隐藏桌宠后计时照常进行，阶段结束通过托盘气泡通知；
- 计时期间小鹞会切换到“专注 / 休息”表情，敲键盘与系统待机不会打扰它；
- 计时状态保存在本机，重启应用后自动恢复。

### 表情行为

| 场景 | 触发条件 |
|---|---|
| 出现 | 启动时 |
| 待机 | 默认状态 |
| 无互动 | 2 分钟未碰宠物 |
| 互动 | 鼠标进入 / 点击宠物 |
| 敲键盘 | 检测到键盘输入 |
| 专注 / 休息 | 番茄钟运行中 |
| 系统待机 | 电脑空闲超过 5 分钟 |
| 消失 | 退出时播放动画 |

## 本次更新（v1.1.0 预览版）

- 新增番茄钟专注计时：25/5、45/10、60/15 预设，支持自定义专注 / 休息 / 长休时长与长休轮数（默认 45/5/15/6）
- 休息结束自动开始下一轮，每 N 轮进入一次长休息；关闭浮层或隐藏桌宠后计时照常，阶段结束托盘气泡通知，重启自动恢复
- 专注卡片：常驻不因失焦消失、可拖动，自定义输入不再被刷新打断
- 敲键盘音符动画：每次按键从宠物顶部弹出随机音符（多种符号 / 颜色 / 动画）
- 样式优化：三档尺寸整体缩小（模型 150 / 200 / 250，容器 200 / 250 / 300，默认小）；颜色面板纯黑改为炭黑（避免透明隐身），尺寸切换真实缩放
- 右键菜单新增“专注”与“关于”入口（关于跳转项目页），菜单晕影更收敛
- 全新哨口造型应用图标（多尺寸），托盘 / 快捷方式 / 安装器同步
- 启动体验：自动清理 WebView2 缓存、首帧即正确尺寸，不再闪现旧模型
- 软件信息：公司 = 东南大学 声绘鹞影实践团，开发人员 = Ainepadma

## 已知限制

- 仅支持 Windows x64 单主屏环境；
- 首次运行需要联网下载 .NET 9 Runtime（如未安装）；
- 预览版仍在打磨，个别场景下表情切换或菜单定位可能不够理想，欢迎反馈。

## 反馈

遇到问题或有建议，请通过 GitHub Issues 提交，并附上：

- 操作系统版本（Win10 / Win11 与版本号）
- 屏幕分辨率与缩放比例
- 问题出现时的操作步骤

源码与构建说明见 [README.md](README.md)。
