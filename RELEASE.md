# 小鹞 WhistleBot Preview v1.3.0

> 版本标识：**WhistleBot Preview v1.3.0**
> 开发人员：**Ainepadma**
> 研发团队：**东南大学 声绘鹞影实践团**
> 版本日期：2026-09-11
> 平台：Windows 10 / 11 x64

Preview v1.3.0 新增六角板鹞与九连星板鹞双击动画，统一行动、今天、日历、管理四张桌面卡片的尺寸与显示，并集中修复 Windows 启动、卸载、窗口交互和日程数据问题。

## 下载

| 文件 | 说明 |
|---|---|
| `DesktopPet-Preview-v1.3.0-win-x64-setup.exe` | 安装包（推荐，包含启动引导器） |
| `DesktopPet-Preview-v1.3.0-win-x64.zip` | 绿色版压缩包，解压后运行 `Bootstrap.exe` |

本地构建产物位于 `dist`。文件名列明本版产物，不表示已上传到发布站点。

## 本次更新

### 双击小鹞，哨口汇成板鹞

- 双击桌宠主体，六角板鹞与九连星板鹞各 50% 概率触发；每次开始独立抽取，播放中重复双击不叠加、不重新抽取。
- 六角板鹞保留 115 枚哨口，九连星保留中央大星、外围八小星与间隙中的八枚哨口；竹骨和彩绘蒙面逐步显现，随风轻摆并飘出音符。
- 参考 Banyao Kites 的真实哨口排列与矩形＋45° 方形结构，沿用小鹞配色和主体哨口形态。
- 简化复刻原 1:1 风筝面模型的朱红人物圆章、祥云、蝴蝶与花鸟图案，改用平涂插画、细描边与轻微正交风摆，去除球面高光。
- 九连星简化复刻青龙金云、外围盘龙和神将圆章，保持双正方形的八尖星结构及 24 处尖顶连接。命中区域按独立面片生成，星面之间透明空隙可点击穿透。
- 风筝面保持当前小鹞主体尺寸的 1.5 倍，三档样式分别映射；修复颜色参数中的 `#` 被网址吞掉及页面就绪前消息丢失导致的重启颜色不一致。
- 约 10 秒后自动恢复；支持拖拽、右键／Esc 收回、专注期间播放以及减少动态效果设置。
- 动画仅临时扩展透明窗口，收回后恢复用户尺寸与当前位置，不修改日程或尺寸配置。

### 首次使用教学

- 第一次成功打开程序时自动显示四步教学，介绍拖动、单击、双击板鹞、键盘音符、日程卡片、专注计时、样式设置和托盘恢复。
- 教学主题色跟随当前小鹞配色，支持按钮与左右方向键翻页、Esc 关闭；成功显示后记录一次性状态，不受尺寸或颜色配置覆盖。
- “设置”子菜单新增“使用说明”，关闭首次教学后仍可随时重新打开。

### 卡片尺寸与显示

- 今天／行动统一为 **400 × 480**，日历／管理统一为 **960 × 680**（逻辑像素）。随 DPI 缩放，屏幕工作区不足时自动收缩。
- 旧布局迁移到两档尺寸，保留位置、固定、显示与置顶状态；行动卡片改为内部滚动，不再随内容伸缩。
- 统一 40 像素标题栏、紧凑卡片 12 像素边距与细滚动条；今天卡片的统计和新建操作保持在底部。
- 管理入口随宽度排列一至三列，移除子页面重复返回栏；修复手动深色主题的背景与文字不一致。

### Windows 与窗口交互

- 启动引导器复用用户目录中的 x64 .NET 9，检查运行时架构、文件完整性与 Desktop/Core 补丁兼容性；缺少 WebView2 时提供安装入口。
- 安装版调用匹配的系统卸载程序；绿色版提示手动移除文件，避免误删同目录下的其他资料。卸载和自启动操作检查当前安装归属。
- 改进负坐标副屏、高 DPI、小屏和断开显示器后的卡片位置恢复；窗口显示状态及时同步，取消过期切换回调。
- 宠物命中区域跟随 SVG 和音符动画，透明空白允许点击穿透；修复长时间开机后的空闲计时回绕及下载链接片段解析。

### 日程与专注

- 重复日程管理返回整个系列，编辑、删除和完成前明确操作范围；保留未修改的复杂重复规则、精确时间与多条提醒。
- 修复含时区偏移的范围查询；ICS 保留时区、全天日期、排除日期及零分钟提醒，正确处理夏令时边界。
- 今天卡片随日程变更与跨日刷新，多个窗口同步设置并拒绝过期查询结果。
- 自定义番茄钟轮数在重启后正确恢复。

## 升级与数据

- 升级前退出旧版本。安装版沿用原安装位置与应用标识；绿色版解压到独立目录后运行 `Bootstrap.exe`。
- 日程、设置和卡片布局保存在用户数据目录；无需删除数据即可升级。首次打开新版时自动调整卡片尺寸。
- 更新仅引导下载，不会在后台替换程序。绿色版菜单中的“卸载”不再自动删除程序目录。

## 更新部署

将随构建输出的 `dist/version.yml` 上传到：

`https://ainepadma.cn/soundflyinggallery/sound/desktop-pet/version.yml`

清单中的 `version: 1.3.0` 用于兼容现有客户端版本比较，`display_version: Preview v1.3.0` 标记预览版名称。安装包、绿色包与下载页准备完成后再上传清单。下载页固定为：

`https://ainepadma.cn/soundflyinggallery/sound/desktop-pet/#download`

## 系统要求

- Windows 10 / 11 64 位
- .NET 9 Desktop Runtime（首次运行由 Bootstrap 引导安装）
- Microsoft Edge WebView2 运行时（缺失时程序提供安装引导）

## 验证

- Windows／日程验收 95 项、番茄钟 12 项、前端回归 7 项、宠物命中桥接 8 项、双击板鹞动画 33 项、使用教学 16 项通过。
- TypeScript 检查、Web 前端构建、Windows 主程序与启动器构建通过。
- 使用隔离数据与配置完成 WebView2 的卡片长列表、空状态、深色主题和小尺寸渲染检查。
- 双击板鹞已通过真实 WebView2 鼠标双击、成形／风摆／音符、透明区域、专注状态保持、自动恢复及 Esc 取消检查。
- 已核验三档风筝面与主体的 1.5 倍比例、彩绘资源加载、启动配色和尺寸、播放中十种样式色的实际绘制像素。
- 九连星已通过真实双击选型、九面与八哨口渲染、三档尺寸、十色像素、内部孔隙的绘制与原生穿透、自动恢复及 Esc 收回检查。
- 使用教学已通过真实 WebView2 四页渲染、主题配色、翻页、最终操作及高 DPI 无溢出检查。

## 已知限制

- 检查更新需要能访问发布站点；网络不可用时启动不会打扰用户，手动检查会提示连接失败。
- 更新检查只引导下载，不会在后台替换正在运行的程序。
- 本版仍为 Preview；上述自动化及隔离渲染检查不替代真实安装／卸载、多显示器不同缩放下的完整人工验收。
